# Sources

`data/sources.json` is the machine-readable source registry. Canonical upstream repositories and official documentation are preferred over mirrors, aggregators, or social posts. Unknown licenses remain `unknown`; absence of a license is never treated as permission to copy full skill text.
