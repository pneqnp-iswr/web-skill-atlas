# Skill schema

Required fields: `id`, `name`, `slug`, `description`, `category`, `subcategory`, `type`, `source_url`, `source_platform`, `quality_score`, `status`, `last_verified`, and `canonical`. Optional metadata includes tags, frameworks, languages, tools, source detail, author, license, discovery trail, mirroring permission, and relationships.
