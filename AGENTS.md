# Agent instructions

`data/skills.json` is canonical. Do not hand-edit generated counts, category pages, indexes, or `data/stats.json`; update canonical data and run `python scripts/generate.py`. Preserve source provenance and verification status. Never upgrade `partially-verified` to `verified` without inspecting the upstream source. Never infer a license. Run validation before committing.
